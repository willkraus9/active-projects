#function = code executed when called (invoke)

def hello(first_name,last_name,age):
    print("Hello, " +first_name +" " +last_name +"!")
    print("You are " +str(age) +" years old.")
    
hello("Will","Kraus",22)
    #info inside = arguments; needs matching parameters
    